# alphapulse — signal, copy-trading, EA & managed-account provider discovery

- **baseUrl:** `https://alphapulse-omega.vercel.app`
- **Coverage:** Global (US-accessible flags, Asia-Pacific desk, region + `lang` params; forex, crypto, indices, commodities)
- **Payment:** x402, USDC on Base. All endpoints $0.10. Call with `bankr x402 call "<baseUrl><path>?<params>"`.

Alternative-trading intelligence: AI-synthesized rankings and due-diligence for signal providers, copy-trading leaders, MT4/MT5 expert advisors (EAs/robots), managed accounts, and DeFi vaults — filtered by drawdown, track record and return. Structured JSON synthesized from provider platforms and market data (MyFxBook-style track records, CoinGecko, CFTC, FRED, EIA) — better than a model guessing. Discovery/vetting only; not financial advice.

## Full alphapulse catalog ($0.10 each)

| Endpoint (path) | Price | Optimized description | Category | Trigger phrases | Required params |
|---|---|---|---|---|---|
| `/api/alpha/discover` | $0.10 | Cross-platform provider discovery — searches every provider type (signal, copy, EA, managed, vault) for an instrument, filtered by max drawdown, minimum track record and target return. Structured JSON. Call when a user asks to find profitable traders/systems to follow for a market. | trading | "find me profitable traders to copy", "best trading systems for forex", "who has good track record low drawdown", "discover alpha providers" | none (`instrument`, `type` recommended) |
| `/api/alpha/signals` | $0.10 | Trading-signal-provider discovery — ranked signal services for an instrument/platform, filtered by drawdown and weeks of verified track record. Structured JSON. Call when a user asks for the best trading signals or signal subscriptions for a market. | trading | "best forex signal providers", "top crypto trading signals", "signal service with low drawdown", "verified trading signals to subscribe" | none (`instrument` recommended) |
| `/api/alpha/ea` | $0.10 | Expert Advisor (MT4/MT5 trading robot) discovery — vetted EAs for an instrument and strategy, filtered by drawdown and verified performance. Structured JSON. Call when a user asks for the best forex robot, MT4/MT5 EA, or automated trading system. | trading | "best MT4 expert advisor", "profitable forex robot", "MT5 EA with verified results", "automated trading system for gold" | none (`instrument`, `strategy` optional) |
| `/api/alpha/copy` | $0.10 | Copy-trading leader discovery — top traders to copy across eToro/ZuluTrade-style platforms for an instrument, filtered by drawdown, region and copier count. Structured JSON. Call when a user asks who to copy or the best copy-trading traders. | trading | "best traders to copy trade", "top eToro traders for crypto", "who should I copy on ZuluTrade", "copy trading leaders low drawdown" | none (`instrument` recommended) |
| `/api/alpha/managed` | $0.10 | Managed-account (PAMM/MAM) discovery — professionally managed programs for an instrument and region, filtered by minimum investment and drawdown. Structured JSON. Call when a user asks about managed forex/crypto accounts or PAMM/MAM programs. | trading | "managed forex accounts", "PAMM account recommendations", "have professionals manage my trading", "MAM programs low drawdown" | none (`instrument`, `region` optional) |
| `/api/alpha/vaults` | $0.10 | DeFi vault discovery — automated on-chain yield/strategy vaults by protocol, strategy and minimum TVL. Structured JSON. Call when a user asks for the best DeFi vaults or automated on-chain strategies to deposit into. | defi | "best DeFi vaults", "automated onchain yield strategies", "vaults with high TVL", "where to deposit for managed DeFi yield" | none (`protocol`, `strategy` optional) |
| `/api/alpha/vet` | $0.10 | Provider due-diligence — a single named signal/copy/EA/managed provider run through a red-flag check: track-record verification, drawdown reality, fee traps, scam markers. Structured JSON. Call when a user asks whether a specific provider or trader is legit before subscribing. | trading | "is this signal provider a scam", "vet this copy trader", "due diligence on this EA seller", "is this trader legit before I subscribe" | `provider` |
| `/api/alpha/broker` | $0.10 | Introducing-broker-approved broker recommendation by region, use case and instrument — reputable, well-regulated brokers that support the strategy. Structured JSON, global. Call when a user asks which broker to use for copy trading, EAs, or a region. | trading | "best broker for copy trading", "regulated broker in my region", "which broker supports MT5 EAs", "broker recommendation for forex" | none (`region`, `use_case` optional) |
| `/api/alpha/asia` | $0.10 | Asia-Pacific copy-trading discovery — providers and platforms serving APAC markets and regulations for an instrument/region. Structured JSON. Call when a user in or targeting Asia asks for copy trading or providers accessible in their region. | trading | "copy trading in Asia", "APAC trading providers", "best copy trading platform in Singapore", "traders to copy from Asia" | none (`instrument`, `region` optional) |
| `/api/alpha/alternative` | $0.10 | Alternative & niche strategy discovery — non-mainstream approaches (arbitrage funds, grid bots, prediction-market strategies, exotic categories) with US-accessibility flag. Structured JSON. Call when a user asks for unusual, niche, or alternative trading strategies beyond signals/copy. | trading | "niche trading strategies", "alternative to copy trading", "grid bot and arbitrage funds", "unusual trading approaches" | none (`category` optional) |
| `/api/alpha/compare` | $0.10 | Side-by-side provider comparison — two or more named providers compared on track record, drawdown, fees and risk. Structured JSON. Call when a user asks which of several signal/copy/EA providers is better or wants a head-to-head. | trading | "compare these two signal providers", "which copy trader is better", "X vs Y trading provider", "head to head EA comparison" | `providers` (comma-separated, min 2) |
