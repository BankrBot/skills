# Veil SDK Reference (Compatibility Note)

The canonical CLI reference now lives at [`../reference.md`](../reference.md).

Use that file for:

- unsigned payload shapes
- SDK function signatures
- CLI command reference
- error codes and deposit minimums

This compatibility file remains so older links inside the skills repo do not break.
